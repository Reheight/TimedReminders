# Database connection — the only required environment variable
DATABASE_URL="mysql://user:password@host:3306/dbname"

# All other settings (VAPID keys, cron secret, app name, session config, PIN)
# are managed inside the app via Settings.
